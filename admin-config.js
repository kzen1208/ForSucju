window.FORMYLOVE_ADMIN_CONFIG = {
  // Paste the deployed webhook URL here.
  // Example:
  // webhookUrl: "https://script.google.com/macros/s/AKfycb.../exec",
  webhookUrl: "",
  projectName: "Formylove",
  // Optional preview mode for the admin UI on the page itself.
  // Open the page with ?admin=1 to see the local status line.
  adminPreviewParam: "admin",
  adminPreviewValue: "1",
};
